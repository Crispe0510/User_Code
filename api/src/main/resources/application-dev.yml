# 开发环境（spring.profiles.active=dev）
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/wy_db?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai
    username: root
    password: 123456
    driver-class-name: com.mysql.cj.jdbc.Driver

mybatis:
  configuration:
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl

logging:
  level:
    com.wy.api: debug
